## 주간 회고

<!-- 매주 금요일 체크인 후 아래에 누적 -->

### 1주차 (8/26~8/28) 회고

**세팅**
- study-log 저장소 신규 구축, logs/projects 구조 확정
- git pull/push 충돌 상황 직접 겪고 해결 (merge, 스마트따옴표 이슈)

**코딩테스트 (프로그래머스/PCCE)**

| 날짜 | 문제 수 | 주요 내용 |
|---|---|---|
| 8/26 | 1 | 두 정수 사이의 합 |
| 8/27 | 7 | 핸드폰 번호 가리기, 문자 출력, 각도 합치기, 수 나누기, 병과 분류, 심폐소생술, 버스, 물 부족 |
| 8/28 | 10+ | 나이 계산, 가습기, 닉네임 규칙, 창고 정리, 출력, 피타고라스, 가채점, 저축, 산책, a/b 출력 |

**배운 것**
- 테스트 통과 ≠ 로직 정답 (물 부족 문제에서 직접 체감)
- 0-index vs 1-index 혼동 패턴 반복 인지 (가채점, 버스)
- Python 타입 변환(`str()`, f-string), 문자열 불변성, `.swapcase()` 등 내장 메서드

### 2주차 (9/1 개강 ~ 9/3) 회고

**코딩테스트 (프로그래머스)**
- 9/1: 특수문자 출력(이스케이프 문자), a+b 출력(f-string 응용), 문자열 순회
- 9/2: 코드 처리하기(mode 상태 전환), 등차수열 항 계산, 2차원 리스트 인덱싱(queries)

**배운 것**
- 역슬래시(`\`)는 탈출문자, 실제 출력하려면 `\\` 필요
- 상태가 바뀌는 문제는 그 상태를 기억할 변수(mode)를 따로 둬야 함
- 2차원 리스트는 `리스트[i][j]`로 접근, `for i, j in queries:`로 언패킹 가능

### 3주차 (개강 후 실습 반영, ~9/18) 회고

**세팅**
- react-apps, jsp-space 신규 저장소 구축 (.gitignore 설정 포함), study-log/projects/README.md에 링크 연결
- study-log에 concepts/ 폴더 신설 — 과목별 개념정리 전용 공간 확보

**스크립트프로그래밍 (React)**
- 컴포넌트 분리, props 전달, 배열 렌더링(`.map()`), 이벤트 핸들링 실습 (basic-app)
- State(useState) 기초, 부모-자식 간 state 공유, `prev =>` 패턴을 통한 정확한 누적 업데이트 (state-app)

**웹프로그래밍 (JSP)**
- HelloJSP에 이어 CafeMenu 프로젝트로 심화 — 세션(session)을 이용한 페이지 간 데이터 공유, 폼 데이터 전달과 처리, Java 클래스(필드/생성자/getter·setter) 설계

**배운 것**
- Node.js/npm/npx/Vite/React/JSX 각각의 역할과 계층 구조를 근본 원리부터 이해 (브라우저 전용이던 JavaScript가 Node.js로 독립 실행 가능해진 배경까지)
- JSP와 JavaScript는 이름만 비슷할 뿐 완전히 다른 언어 (Java 계열 vs JavaScript 계열)
- 여러 언어/프레임워크에서 반복되는 공통 패턴 발견: 값을 직접 바꾸지 못하게 하고 정해진 통로(setState / getter·setter)로만 접근하게 하는 설계